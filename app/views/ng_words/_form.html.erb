<%= form_for(@ng_word) do |f| %>
  <% if @ng_word.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@ng_word.errors.count, "error") %> prohibited this ng_word from being saved:</h2>

      <ul>
      <% @ng_word.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :word %><br>
    <%= f.text_field :word %>
  </div>
  <div class="field">
    <%= f.label :point %><br>
    <%= f.number_field :point %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
